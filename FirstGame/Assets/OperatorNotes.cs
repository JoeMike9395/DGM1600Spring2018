using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OperatorNotes : MonoBehaviour {

	// The . is the go into box operator. (Public.Animal.Hippo)
	// The logic function is a box that needs to be called.
	// public int "Thing / Box"; will give a value to code with.
	// The And and Or operators in if then statements can be && or ||
	// public string and public int are different. Use them for what they are. (Number, String)
	// Heart = 100;
	// Health += 10; (Add to the ASSIGNED value)
}